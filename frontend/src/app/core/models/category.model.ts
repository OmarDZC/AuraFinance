/**
 * Refleja com.aura.finance.dto.category.CategoryResponse (backend).
 */
export interface Category {
  id: number;
  name: string;
}

/**
 * Refleja com.aura.finance.dto.category.CategoryRequest (backend).
 */
export interface CategoryRequest {
  name: string;
}
